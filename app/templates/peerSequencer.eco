<div id="holder<%= cleanId(@sequencer.id) %>">
  <span id="n<%= cleanId(@sequencer.id) %>"><%= @sequencer.name %></span> is playing pattern <span id="s<%= cleanId(@sequencer.id) %>"><%= @sequencer.pattern %></span>
</div>
